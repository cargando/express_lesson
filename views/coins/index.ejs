<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Node js Express Tutorial</title>
    <% include ./inc.ejs %>
</head>
<body>
<% include ./navbar.ejs %>

<div class="container">
    <div class="row">
        <div class="col-md-12">
            <h1>List of curencies</h1>
        </div>
    </div>
    <table class="table table-striped">
        <thead>
        <tr>
            <td><b>Coin Name</b></td>
            <td><b>Coin Price</b></td>
            <td colspan="2"><b>Action</b></td>
        </tr>
        </thead>
        <tbody>
        <% for(var i=0; i < coins.length; i++) { %>
            <tr>
                <td><%= coins[i].name %></td>
                <td><%= coins[i].price %></td>
                <td><a href="/coins/edit/<%= coins[i]._id %>" class="btn btn-sm btn-primary">Edit</a>
                <a href="/coins/delete/<%= coins[i]._id %>" class="btn btn-sm btn-danger">Delete</a></td>
            </tr>
        <% } %>
        </tbody>
    </table>
    <hr />
    <a href="/coins/create" class="btn btn-primary" role="button">Create Currency</a>
</div>
<% include ./footer.ejs %>
</body>
</html>